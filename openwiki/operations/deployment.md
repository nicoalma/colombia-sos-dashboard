---
type: Playbook
title: Deployment & automation
description: How the site deploys to GitHub Pages on push to main, and how the scheduled OpenWiki workflow keeps this wiki current via documentation PRs.
tags: [operations, ci, github-pages]
timestamp: 2026-08-14T00:00:00Z
openwiki:
  roles: [operations, delivery]
  source_paths: [.github/workflows/pages.yml, .github/workflows/openwiki-update.yml]
  validation_commands: ["Actions tab → both workflows green after a push to main"]
---

# Deployment & automation

## GitHub Pages (`.github/workflows/pages.yml`)

Every push to `main` (or a manual `workflow_dispatch`) uploads the repository root as a Pages artifact and deploys it. `actions/configure-pages` runs with `enablement: true`, so the first successful run turns Pages on by itself — no manual settings step. There is no build: the repo root *is* the site, which is why generated docs (`openwiki/`) being plain Markdown is harmless — they are just extra static files.

Note: Pages on a **private** repository requires a paid GitHub plan; on a free account, make the repository public or the deploy job will fail with an entitlement error.

## OpenWiki updates (`.github/workflows/openwiki-update.yml`)

Generated by `openwiki --init` (v0.3.3, Anthropic provider). Daily at 08:00 UTC — or manually via `workflow_dispatch` — it runs `openwiki code --update --print`, which diffs the repo against the last documented commit and refreshes `openwiki/`, `AGENTS.md`, and `CLAUDE.md`, opening a `docs: update OpenWiki` PR when something changed.

**Required secret:** `ANTHROPIC_API_KEY` (Settings → Secrets and variables → Actions). Without it the scheduled run fails at the OpenWiki step. The LangSmith secrets in the workflow are optional tracing extras.

To update docs from a laptop instead: `npm i -g openwiki && openwiki --update` with `ANTHROPIC_API_KEY` set locally.
